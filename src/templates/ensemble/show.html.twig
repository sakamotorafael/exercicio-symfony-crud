{% extends 'base.html.twig' %}

{% block title %}Ensemble{% endblock %}

{% block body %}
    <h1>Ensemble</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ensemble.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ensemble.name }}</td>
            </tr>
            <tr>
                <th>PartsCount</th>
                <td>{{ ensemble.partsCount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ensemble_index') }}">back to list</a>

    <a href="{{ path('ensemble_edit', {'id': ensemble.id}) }}">edit</a>

    {{ include('ensemble/_delete_form.html.twig') }}
{% endblock %}
