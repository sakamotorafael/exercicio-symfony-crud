{% extends 'base.html.twig' %}

{% block title %}Oeuvre{% endblock %}

{% block body %}
    <h1>Oeuvre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ oeuvre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ oeuvre.name }}</td>
            </tr>
            <tr>
                <th>Opus</th>
                <td>{{ oeuvre.opus }}</td>
            </tr>
            <tr>
                <th>Tonality</th>
                <td>{{ oeuvre.tonality }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ oeuvre.genre }}</td>
            </tr>
            <tr>
                <th>CatalogueNumber</th>
                <td>{{ oeuvre.catalogueNumber }}</td>
            </tr>
        </tbody>

        <h2> {{ completeName }} </h2>
    </table>

    <a href="{{ path('oeuvre_index') }}">back to list</a>

    <a href="{{ path('oeuvre_edit', {'id': oeuvre.id}) }}">edit</a>

    {{ include('oeuvre/_delete_form.html.twig') }}
{% endblock %}
